name = "Format TypeScript file"

[setup.files."example.ts"]
content = """
function hello(name:string):string{
return "Hello, "+name
}
"""

[[steps]]
run = "sand format example.ts"

[steps.expected]
exit-code = 0

[steps.expected.files."example.ts"]
content = """
function hello(name: string): string {
  return "Hello, " + name;
}
"""
